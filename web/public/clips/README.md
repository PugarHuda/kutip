# Finale pitch clips

Three short, silent screen-recordings the presenter narrates over on
`/slides`. No live demos by Encode's rule — these are pre-rendered.

| File          | Length | Shot list |
|---------------|--------|-----------|
| `flow.mp4`    | ~15 s  | `/research` → type a query → click Pay → 5-step progress completes → receipt appears. Speed-up 2× the slow middle if needed. |
| `payout.mp4`  | ~12 s  | Receipt "Authors paid" table visible → mouse hovers a row → click tx hash → KiteScan opens, ERC-20 Transfer events listed. |
| `verify.mp4`  | ~12 s  | `/dashboard/history` list → click a row → `/verify/<queryId>` → scroll to "Summary digest · keccak256" → click "Download JSON receipt". |

**Encoding:** 1080p, H.264 mp4, **silent** (audio strips the voiceover collision), looped by the deck. Keep each ≤15 s — the deck loops, the presenter explains.

**Recording shortcut:** Windows `Win+G` (Game Bar) → record window → export → drop here under the exact filename above. Slides auto-pick it up; placeholder shot-list disappears once the file resolves.
